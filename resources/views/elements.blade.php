<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ $title ?? 'API Documentation' }}</title>
    <link rel="stylesheet" href="https://unpkg.com/@stoplight/elements@9/styles.min.css">
    <style>
        body {
            margin: 0;
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
            background: #f7f8fa;
        }
        .spectrum-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: #fff;
            padding: 20px 40px;
        }
        .spectrum-header h1 {
            margin: 0;
            font-size: 24px;
        }
        .spectrum-header .version {
            margin-left: 10px;
            font-size: 12px;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 999px;
            padding: 2px 10px;
        }
        .spectrum-header p {
            margin: 8px 0 0;
            opacity: 0.9;
            font-size: 14px;
        }
        .spectrum-footer {
            text-align: center;
            padding: 16px;
            color: #666;
            font-size: 12px;
        }
    </style>
</head>
<body>
    <div class="spectrum-header">
        <h1>
            {{ $title ?? 'API Documentation' }}
            <span class="version">v{{ $version ?? '1.0.0' }}</span>
        </h1>
        @if(!empty($description))
        <p>{{ $description }}</p>
        @endif
    </div>

    <elements-api
        id="elements-api"
        layout="{{ $elementsLayout ?? 'sidebar' }}"
        router="{{ $elementsRouter ?? 'hash' }}"
        hideTryIt="{{ $elementsHideTryIt ? 'true' : 'false' }}"
    ></elements-api>

    <div class="spectrum-footer">
        Generated by <a href="https://github.com/wadakatu/laravel-spectrum" target="_blank">Laravel Spectrum</a>
        on {{ $generatedAt ?? date('Y-m-d H:i:s') }}
    </div>

    <script src="https://unpkg.com/@stoplight/elements@9/web-components.min.js"></script>
    <script>
        const spec = {!! $spec !!};
        document.getElementById('elements-api').apiDescriptionDocument = spec;
    </script>
</body>
</html>
